# Air Mouse 네트워크 범위 정리

## 결론

학교망, 회사망, 공용 Wi-Fi처럼 장치 간 직접 통신을 막는 네트워크는 구현 대상에서 제외한다.

Air Mouse는 Windows PC에서 HTTP/WebSocket 서버를 열고, 휴대폰이나 태블릿 브라우저가 같은 네트워크에서 `http://PC_IP:포트`로 접속하는 구조를 유지한다. PC와 조작 기기가 서로 직접 접근 가능한 네트워크를 전제로 한다.

## 지원 범위

- 개인 공유기 Wi-Fi
- PC 유선 LAN + 같은 공유기의 Wi-Fi
- 휴대폰 핫스팟에 PC가 접속한 환경
- Windows 모바일 핫스팟에 휴대폰이 접속한 환경
- 같은 사설망에서 PC IP와 포트에 직접 접근 가능한 환경

## 제외 범위

- 학교망
- 회사망
- 게스트 Wi-Fi
- 기숙사/공용망 중 장치 간 통신이 막힌 망
- AP isolation/client isolation이 켜진 네트워크
- 방화벽 또는 네트워크 정책상 PC 포트 접근이 차단된 환경

이 환경들은 앱 내부 로직으로 안정적으로 우회하지 않는다.

## Bluetooth 판단

Bluetooth는 이번 구현 범위에서 제외한다.

이유:

- 현재 앱은 브라우저 기반 컨트롤러라 Bluetooth와 구조가 맞지 않는다.
- Web Bluetooth는 지원 브라우저와 보안 제약이 커서 범용 리모컨 용도로 안정적이지 않다.
- Android 전용 앱과 Windows Bluetooth 수신 로직을 새로 만들어야 하므로 현재 제품 범위를 크게 벗어난다.
- iPhone까지 동일하게 지원하기 어렵다.

## 최종 방향

Air Mouse는 LAN/WebSocket 기반의 가벼운 Windows용 리모컨으로 마무리한다. 학교망처럼 정책상 막힌 네트워크는 문서로 안내하고, 구현에서 우회하지 않는다.
