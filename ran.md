# LAN / Wi-Fi 동작 정리

## 결론

Air Mouse는 Wi-Fi 전용 앱이 아닙니다. Windows PC에서 HTTP/WebSocket 서버를 열고, 휴대폰이나 태블릿이 `http://PC_IP:포트`로 접속하는 구조입니다. PC가 랜선(Ethernet/LAN)으로 연결되어 있어도 조작 기기에서 그 PC IP에 접근할 수 있으면 동일하게 동작합니다.

학교/회사/공용망처럼 장치 간 직접 통신이 막힌 네트워크는 구현 지원 범위에서 제외합니다. 이런 망에서는 Air Mouse가 정상 실행되고 Windows 방화벽이 열려 있어도 휴대폰이 PC 주소에 접근하지 못할 수 있습니다.

## 이번 환경에서 확인된 내용

실행 로그의 `http://172.18.199.69:5000`은 현재 PC의 `Intel(R) Ethernet Connection (12) I219-V` 랜카드 주소입니다. 즉, 이번 경우에는 Docker/WSL 같은 가상 어댑터 IP가 아니라 실제 유선 LAN IP가 선택된 상태입니다.

PC 브라우저에서 `Client connected: 127.0.0.1:...`가 찍히는 것은 QR 페이지가 PC 안에서 열린 것이므로 정상입니다. 이것만으로 휴대폰이 같은 주소에 접근 가능하다는 뜻은 아닙니다.

## 휴대폰에서 무한 로딩이 걸리는 대표 원인

- 휴대폰이 PC와 같은 네트워크가 아니라 모바일 데이터나 게스트 Wi-Fi를 사용 중인 경우
- 휴대폰 IP 대역이 `172.18.199.x`가 아닌 다른 대역인 경우
- 공유기에서 게스트망 격리, AP isolation, client isolation이 켜진 경우
- Windows Defender 방화벽이 `AirMouse.exe` 또는 TCP `5000` 포트를 차단하는 경우
- 회사/학교/공용망처럼 장치 간 내부 통신이 막힌 네트워크인 경우

## 확인 순서

1. 휴대폰 Wi-Fi 상세 정보에서 IP가 PC와 같은 대역인지 확인합니다. 현재 PC 기준으로는 `172.18.199.x`여야 가능성이 높습니다.
2. 휴대폰 브라우저에서 `http://172.18.199.69:5000/health`를 열어 `ok`가 뜨는지 확인합니다.
3. `ok`도 안 뜨면 앱 문제가 아니라 네트워크 접근 또는 방화벽 문제일 가능성이 큽니다.
4. Windows 방화벽에서 Air Mouse 실행 파일 또는 TCP 포트 `5000`을 개인 네트워크에 허용합니다.

## 방화벽 자동 허용 한계

Windows는 일반 권한으로 실행 중인 앱이 방화벽 인바운드 허용 규칙을 조용히 추가하는 것을 막습니다. Air Mouse는 실행 시 실행 파일 기준 `Air Mouse` 규칙과 현재 TCP 포트 기준 `Air Mouse Port` 규칙을 자동 등록하려고 시도하지만, 관리자 권한이 아니면 실패할 수 있습니다.

완전히 별도 설정 없이 쓰려면 `AirMouse.exe`를 관리자 권한으로 한 번 실행해 방화벽 규칙을 만들거나, Windows 방화벽/UAC 허용 창에서 승인해야 합니다. 관리자 승인 없이 모든 기기 접속을 자동 허용하는 방식은 Windows 보안 정책상 안정적으로 구현할 수 없습니다.

관리자 권한으로 실행했는데도 휴대폰에서 접속되지 않는다면, 먼저 휴대폰 브라우저에서 `http://PC_IP:포트/health`를 열어 확인합니다. PC에서는 `/health`가 열리는데 휴대폰에서만 열리지 않으면 앱 실행이나 Windows 방화벽보다 공유기/네트워크의 장치 간 통신 차단 가능성이 큽니다.

학교망처럼 장치 간 통신을 차단하는 환경은 Air Mouse에서 우회 구현하지 않습니다. 필요한 경우 개인 핫스팟, Windows 모바일 핫스팟, 개인 공유기처럼 PC와 조작 기기가 서로 직접 접근 가능한 네트워크를 사용해야 합니다.

## 로직 보완 내용

- Air Mouse는 Wi-Fi 이름에 의존하지 않고 실제 IPv4 후보를 평가합니다.
- 169.254.x.x, loopback 주소는 제외합니다.
- VPN, Docker, WSL, Hyper-V, VMware, VirtualBox, Tailscale 등 가상/터널 어댑터는 낮은 우선순위로 둡니다.
- 기본 포트 `5000`이 사용 중이면 `5001`부터 빈 포트를 찾아 실행합니다.
- `/health` 확인 경로를 추가해 휴대폰에서 네트워크 도달 여부를 빠르게 확인할 수 있게 했습니다.

## 직접 랜선 연결 참고

PC와 휴대폰을 일반 USB 케이블로 연결하는 것은 LAN 연결과 다릅니다. 휴대폰이 USB-C Ethernet 어댑터 등을 통해 실제 이더넷 네트워크에 붙고 IP를 받는 환경이면 가능하지만, 일반 충전/데이터 케이블만으로는 현재 방식과 동일하게 동작하지 않습니다.
