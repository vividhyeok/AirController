# 연결 문제 진단

## 빠른 확인

1. PC에서 열린 연결 화면에 사용 중인 어댑터가 표시되는지 확인한다.
2. 휴대폰이 연결된 방식과 같은 카드의 `http://IP:포트/health`를 연다.
3. `ok`가 보이면 네트워크와 방화벽은 정상이며 브라우저/WebSocket 문제를 확인한다.
4. 열리지 않으면 방화벽의 `AirController Remote` 규칙과 장치 간 통신 허용 여부를 확인한다.

## 구성별 확인

- PC 유선 + 휴대폰 Wi-Fi: 두 장치가 같은 공유기와 사설 IP 대역에 있어야 한다.
- Bluetooth PAN: 페어링만으로는 부족하다. 휴대폰 테더링/개인용 핫스팟과 Windows의 PAN 연결이 모두 활성화되어야 한다.
- VPN/오버레이: 휴대폰과 PC가 같은 VPN에 접속해 있고 피어 간 접속이 허용되어야 한다.
- 학교·회사·게스트망: AP isolation 또는 정책으로 장치 간 통신이 차단될 수 있다.

## 방화벽 허용 창이 반복되던 이유

이전 코드는 앱 시작 때 기존 규칙을 삭제하고 현재 EXE 경로와 선택된 포트로 다시 등록했다. 일반 권한 실행, EXE 위치 변경, 포트 변경이 겹치면 규칙이 없거나 서로 다른 상태가 되어 Windows가 반복적으로 확인할 수 있었다.

현재 코드는 시작 시 방화벽을 수정하지 않는다. 연결 화면의 `한 번만 허용` 버튼을 사용하면 `5000-5019` 포트와 `LocalSubnet` 범위를 가진 규칙 하나만 만든다.

## 잘못된 주소가 보일 때

연결 화면을 새로고침하면 활성 어댑터를 다시 탐색한다. Docker, WSL, Hyper-V 같은 내부 가상 어댑터는 자동으로 숨기며, 유선 LAN·Wi-Fi·Bluetooth PAN·VPN 주소는 각각 구분해 표시한다.
